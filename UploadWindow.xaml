<Window x:Class="MobiDude_V2.UploadWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Upload is running..." Height="400" Width="600" WindowStartupLocation="CenterScreen"
        Icon="appicon.ico">
    <Grid Margin="10">
        <TextBox Name="OutputTextBox" 
                 IsReadOnly="True" 
                 VerticalScrollBarVisibility="Auto" 
                 HorizontalScrollBarVisibility="Auto" 
                 TextWrapping="Wrap"
                 FontFamily="Consolas"
                 FontSize="12"/>
    </Grid>
</Window>
